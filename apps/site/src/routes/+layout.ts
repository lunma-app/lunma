// Prerender the whole site to static output (adapter-static). This single flag
// applies site-wide — every route is built to plain HTML with no server runtime.
export const prerender = true;
export const ssr = true;
export const csr = true;
