// A build-time render surface for the 1200×630 OG image. It is prerendered like
// the rest of the site; the static `static/og.png` is produced by screenshotting
// this route (see the redesign-marketing-site change). Not linked from the nav.
export const prerender = true;
