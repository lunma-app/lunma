# Tasks — smart-folders

## 1. Schema, types, and bus contract

- [x] 1.1 Widen `PinNode` in `shared/types.ts` with the `smart` variant (`{ kind: 'smart', id, name, icon, source: 'gitlab', baseUrl, query, refreshMinutes }` + the `SmartQuery` type); add the ephemeral `AppState.smartFolders` slice (`SmartFolderRuntime` / `SmartFolderItem` types per design D2).
- [x] 1.2 Schema v2 (`shared/schemas.ts` + `shared/migrations.ts` + `shared/chrome/storage.ts`): widen the persisted `PinNodeSchema`, rename/extend to `AppStateV2Schema`, point the schema-to-type coherence assertion at it, bump `CURRENT_SCHEMA_VERSION` to 2, append the `{ toVersion: 2 }` pass-through migration, re-point `readPersistedState`/`salvagePersistedState` (signature `AppStateV2 | null`) at the v2 schema, and exclude `smartFolders` from `Persisted<T>`/`persist()` exactly like `liveTabsById`. Migration tests: v1 envelope migrates losslessly + writes back as v2; persist omits `smartFolders`; a v2 state with a smart node round-trips without quarantine; a pinned tree containing a smart node survives slice-wise salvage intact when an unrelated slice is malformed.
- [x] 1.3 Bus (`shared/bus.ts`): the four lifecycle commands (`createSmartFolder`, `updateSmartFolder`, `deleteSmartFolder`, `refreshSmartFolder`) with flat Zod payloads (`query` as `z.enum`; update/delete/refresh carry `{ spaceId, folderId }` per the folder-command convention), the `SIDEBAR_COMMAND_KINDS` + exhaustiveness updates, and the `smart` member on the bus `PinNodeSchema` mirror so `reorderPinned` round-trips smart nodes. Schema unit tests for the new payloads + the smart-node round-trip.
- [x] 1.4 Icon: add `'folder-git-2'` to `ICON_NAMES` in `shared/icon-names.ts` and regenerate `ui/icon-loaders.generated.ts` (`pnpm gen:icons`); the loader-coverage test stays green.

## 2. Connector engine (background)

- [x] 2.1 `shared/connectors.ts` (new): the `lunma.connectors` `chrome.storage.local` record — `readConnectors()` / `setConnectorToken(host, token | null)`; tokens never logged, never broadcast, never in sync. Unit tests for read/write/clear and malformed-record defaults.
- [x] 2.2 `background/smart-folders.ts` (new) — fetch + normalize: the three canned queries → documented `/api/v4/merge_requests` GET params (`scope=created_by_me` / `scope=assigned_to_me` / `scope=all&reviewer_id=<me>` — `scope=all` is required for review-requested; per_page=20 cap), `/api/v4/user` me-resolution cached per poll cycle, pipeline→tone mapping (`success→ok`, `failed→fail`, `running|pending|created→pending`, `canceled|skipped→warn`, none or any unmapped status → no status), bounded per-MR pipeline enrichment (concurrency ≤5, skipped when the list already carries it), and `baseUrl` handling (PAT host = `new URL(baseUrl).host`; subpath instances work via string-append). Unit tests with faked fetch for each query (asserting the review-requested URL carries `scope=all`), the cap, the tone mapping incl. an unmapped status, the enrichment skip, and a subpath baseUrl.
- [x] 2.3 Auth ladder in the same module: PAT (Bearer + `credentials: 'omit'`) when a token exists for the host, else `credentials: 'include'`; response-shape signed-out detection (401 / redirect-to-HTML / non-JSON → `signed-out`; network/5xx/429 → `error`), never throwing. Unit tests for all ladder rungs and failure shapes.
- [x] 2.4 Scheduling + single-writer wiring: the `lunma/smart-folders-poll` repeating alarm at the min cadence (retuned on create/update/delete, cleared when no smart folders — design D4), per-folder due-clocks, the parallel `'lunma/state-request'` refresh-kick listener (never claims the response channel; `state-snapshot-handler.ts` untouched — see the chrome-event-coordination delta), fetches off-drain enqueueing `{ source: 'connector', kind: 'smartFolders.result' }` (the new `PendingEvent` source member + handlers-map + `EventPolicy` entries), and the `setSmartFolderRuntime` store mutator (pending preserves last-known items). Unit tests: due-folder selection, alarm retune/clear, result event → one broadcast, pending-keeps-items, kick-doesn't-block-snapshot.
- [x] 2.5 Coordinator handlers (design D12): `createSmartFolder` (SW-minted id + `icon: 'folder-git-2'`, baseUrl normalize/validate per spec, cadence clamp to 5, `store.addSmartFolder` inserting at the top of the pinned list like `createFolder`, retune alarm, immediate first fetch), `updateSmartFolder` (`store.updateSmartFolder(spaceId, folderId, config)`; baseUrl/query change → immediate refetch), `deleteSmartFolder` (`store.deleteSmartFolder` removes node + drops runtime, then retune/clear alarm), `refreshSmartFolder` (unconditional; acks ok before the fetch resolves) — update/delete/refresh address `{ spaceId, folderId }`. Error contract tests: unknown spaceId/folderId reject; invalid baseUrl rejects; fetch failures never reject an ack.

## 3. Sidebar rendering

- [x] 3.0 Extend `ui/FolderRow.svelte` (+ harness + test): an optional trailing `badge`, a `menuItems` override (when provided, replaces the built-in folder action morph), a `busy` flag (spins the glyph at the 0.8s-linear cadence; static `--text-dim` under reduced motion), and pass-through `panel`/`panelTitle`/`onPanelBack` forwarded to RowMenu's existing drill-in API — the existing folder usage is byte-for-byte unaffected. Unit tests: badge renders, menuItems override replaces the built-ins, busy spin + reduced-motion static, forwarded panel drills with a back-header, default behavior unchanged.
- [x] 3.1 `sidebar/SmartFolder.svelte` (+ harness + test): composes the extended `FolderRow` (shared icon/title columns, chevron gutter, Space-colour glyph tint, trailing count badge with `20+` at the cap) branching from `PinnedTabs.svelte` on `kind === 'smart'`; expanded result rows at the child inset — leading instance favicon, ellipsized title, at most ONE trailing status dot from the existing semantic tokens, full status phrase in `Tooltip` + ARIA label. Activate-only rows dispatching `openUrl`; menu carries Refresh now · Edit… · Move up/Move down (disabled at the respective list end, dispatching full-tree `reorderPinned` — the keyboard-reachable reorder pair) · Delete (no two-step confirm). Unit tests: populated render + one-glyph restraint, badge values, openUrl dispatch, menu Move up/down dispatch + end-disabling, reorder round-trip.
- [x] 3.1b Smart-node drag semantics in the drag controller (design D13): the smart node drags/reorders among pins as one unit; smart rows are never reported via `targetOntoId` (a drop on one degrades to nearest-edge top-level insertion, never files or mints a folder); smart-to-temporary is rejected like a folder. Unit tests: onto-smart insertion, no folder minting, temp rejection, smart-node reorder round-trip.
- [x] 3.2 Calm states in `SmartFolder.svelte`: first-fetch pending → three static ghost rows; `signed-out` → one "Sign in to ⟨host⟩" row dispatching `openUrl(baseUrl)`; `error` → last-known items + dim "Couldn't reach ⟨host⟩" note row. Reduced motion: no rotating refresh indicator, instant item swaps. Unit tests for all three states.
- [x] 3.3 `sidebar/SmartFolderEditor.svelte` (+ harness + test): the menu drill-in panel (back-arrow header) composing `TextInput` (base URL, default `https://gitlab.com`, validated as an absolute http(s) URL with a quiet inline message blocking confirm; name, auto-suggested from query), `SegmentedControl` (query), and `Select` (cadence 5/10/30/60), plus the Settings → Connectors hint; "New smart folder…" entry in the pinned-header kebab beside "New folder"; Edit… drills into the same panel pre-filled. Unit tests: create dispatch with panel values, edit dispatch, query-change refetch expectation (the dispatched payload), invalid-URL blocks confirm, source not rendered as a choice.

## 4. Options Connectors section

- [x] 4.1 The Connectors card in `options/Options.svelte` (custom `storage.local`-backed section, design D10): host rows with token-set indicator, add/replace/clear via `setConnectorToken`, password-type input, stored tokens never echoed ("Token set — replace?" affordance). Unit tests: add/replace/clear flows and the no-echo guarantee.

## 5. Verification + docs

- [x] 5.1 Update `docs/04-capabilities.md`: the new smart-folders capability section, the sidebar pinned-section composition bullet (smart node kind), and the options-page description (Connectors section).
- [x] 5.1b Update `docs/03-architecture.md`: the `PinNode` union prose gains the `smart` kind; the storage-schema section moves from the v1-empty-migrations baseline to v2 with the first real (pass-through) migration; the ephemeral-slice paragraph gains `smartFolders` alongside `liveTabsById`; correct the section's stale `AppStateV5Schema` / `CURRENT_SCHEMA_VERSION = 5` / `schemaVersion: 5` remnants (pre-existing drift from the baseline reset) while in there.
- [x] 5.2 `pnpm verify` green at the root; `pnpm test:e2e` green; store branch coverage stays >90% with the new mutators covered (the standing store-coverage bar); dev-load eyeball pass against the real work GitLab instance — cookie path, PAT path, signed-out row, pipeline dots at all three tints, reduced motion — screenshots for the review record.
