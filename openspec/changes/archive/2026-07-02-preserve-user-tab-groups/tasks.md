# Tasks: preserve-user-tab-groups

## 1. Store mutators (state-only, chrome-free)

- [x] 1.1 Add `releaseTabsInUntrackedGroups(tabGroupById: ReadonlyMap<TabId, number>): void` to `apps/extension/src/shared/store.svelte.ts`: per window, remove from every instance's `tempTabIds` (and `tempTabTitles`) any tab whose live group (per `tabGroupById`) is ≥ 0 and recorded by no instance in that window. Synchronous, state-only, idempotent; bound records untouched.
- [x] 1.2 Add `onTabGroupIdChanged(tabId: TabId, groupId: number): void` to `apps/extension/src/shared/store.svelte.ts`: `groupId` tracked by Space X's instance in the tab's window → assign to X (`assignSpaceTabs` semantics; early-return no-op when X already solely owns the tab — no reorder); `groupId` ≥ 0 untracked → release the tab from every instance's `tempTabIds`/`tempTabTitles` in its window; `groupId === -1` → no-op.
- [x] 1.3 Unit tests for both mutators in the store test suite: release strips only untracked-group tabs (tracked-group + ungrouped tabs untouched), release idempotence, tracked-move reassignment, already-owned no-op (membership AND order unchanged), `-1` no-op, bound tabs never released/moved, per-window scoping (same Space in two windows unaffected).

## 2. Boot seeding exclusion

- [x] 2.1 In `apps/extension/src/background/seed-existing-tabs.ts`, skip (do not seed) any tab whose `groupId` ≥ 0 has no entry in `spaceByGroupByWindow` for its window — no `onTabCreated` fallback; `ensureSpaceInstance` still runs for the window. Ungrouped tabs and tracked-group tabs keep today's paths exactly. Update the module docstring (group-aware paragraph).
- [x] 2.2 Extend `apps/extension/src/background/seed-existing-tabs.test.ts`: a tab in an unmapped live group is not seeded into any instance; its window still gets an instance; ungrouped fallback and tracked-group seeding regressions stay green.

## 3. Boot reconciliation: release step + sweep exclusion

- [x] 3.1 In `apps/extension/src/background/tab-group-adoption.ts` (`reconcileTabGroupsOnBoot`), call `store.releaseTabsInUntrackedGroups(tabGroupById)` after `adoptExistingGroups` and before `materializeActiveGroups`, reusing the pass's existing `tabGroupById` map. Update the step-order documentation in the module and function docstrings (convert → adopt → release → materialize → ungroup-favorites → duplicate cleanup) and strengthen the invariant wording: the pass never moves a tab out of a live untracked group.
- [x] 3.2 In `materializeActiveGroups`, exclude from the stray-sweep (and from a fresh group's initial membership) any member whose current live group (per `tabGroupById`) is ≥ 0 and not in `trackedGroupIdsForWindow(store, windowId)` — sweep only ungrouped members, members in the Space's own dead/stale group, or members in another tracked group. The exclusion applies to the merged member set (temp + bound + home tabs).
- [x] 3.3 Extend `apps/extension/src/background/tab-group-adoption.test.ts` (`reconcileTabGroupsOnBoot — materialization` + a new release describe): untracked-group member excluded from the sweep; ungrouped/dead-group/other-tracked-group members still swept; home tab in an untracked group not swept while ungrouped home tabs are; release strips a lingering temp tab in an untracked group after adoption; release runs AFTER adoption so a restored tracked group's members are NOT released (restart id re-bind case); full-pass scenario mirroring the repro (formerly-tracked tabs in a user group: group untouched, tabs released, both groups present at the end). Also added: a regression test proving the fresh-install same-title group-fold scenario is unaffected (D3/D5 skipped on that boot — see design.md D3's "Implementation-discovered refinement").

## 4. Mid-session groupId-change handling

- [x] 4.1 In `apps/extension/src/background/handlers/chrome-tabs.ts` (`tabs.onUpdated`), stop early-returning when `changeInfo.groupId` is the only change; forward it to `store.onTabGroupIdChanged(tabId, changeInfo.groupId)` and `ctx.markDirty()` when state changed. Keep the existing url/status/title/favIconUrl behaviour byte-for-byte otherwise.
- [x] 4.2 Handler/coordinator tests (alongside the existing `tabs.onUpdated` coverage): temp tab moved into an untracked group is released; tab moved into a tracked group reassigns to that Space; `groupId: -1` is a no-op; a groupId-only event does not trigger the navigation/dedup/home-tab-adoption paths.

## 5. End-to-end repro guard

- [x] 5.1 Add the Playwright e2e `apps/extension/e2e/user-group-survives-restart.spec.ts`, following the `_persist.spec.ts` relaunch pattern (`chromium.launchPersistentContext` against a shared temp profile dir, close the context, relaunch with `--restore-last-session`, wait for the SW's boot reconcile to settle). Two `test()` cases (one per confirmed variant), each:
  - **Phase 1 (build the state):** launch, create 3 plain tabs (`chrome.tabs.create`) so the active Space "Default" tracks them as temp tabs; wait for them to settle; read the Space's live group's colour off `chrome.tabGroups.query({})` (needed for the same-colour variant); group two of the tracked tabs into a NEW native group via `chrome.tabs.group({ tabIds })` + `chrome.tabGroups.update(groupId, { title, color })` — **variant A:** title `"Default"`, the Space group's own colour (worst-case collision); **variant B:** title `"Mine"`, colour `"orange"` (unrelated identity); wait for the mid-session `tabs.onUpdated` groupId-change reconciliation to process; dump `chrome.storage.local.get('lunma.state')` and assert the two tabs are already absent from every instance's `tempTabIds` (confirms D6 fired before restart, keeping the scenario within the bounded guarantee — see design.md D2b); close the context.
  - **Phase 2 (restart + assert):** relaunch the same profile dir with `--restore-last-session`; wait for boot; query `chrome.tabGroups.query({})`/`chrome.tabs.query({})` (NOT scoped to the pre-restart windowId — a real restart opens the restored session as a NEW window with a new id) and assert exactly two groups exist. Identify "the user's own group" as the one NOT recorded by any instance's `groupId` (title/colour alone cannot disambiguate in variant A, where the Space's own restored group shares both) and assert it still holds 2 tabs and neither tab is listed in any instance's `tempTabIds`. Fail loudly (not silently) if either group is missing or the user's group's tab count dropped — that is exactly the regression this change fixes.
- [x] 5.2 Run `pnpm build` + `pnpm test:e2e` and confirm the new spec passes alongside the existing smoke (notably `space-group-collapse.spec.ts` and `_persist*.spec.ts` stay green — `space-group-collapse.spec.ts`'s single scenario is a pre-existing `test.fixme`, unrelated to this change).

## 6. Docs + verification

- [x] 6.1 Update `docs/architecture.md`: the "Boot order" diagram note, the `seedExistingTabs` paragraph (untracked-group skip), the "Boot group reconciliation" numbered steps (insert the release step, describe the sweep exclusion and the strengthened never-drain invariant, and the fresh-install-conversion exemption), and the `tabs.onUpdated` bullet under "Registered Chrome listeners" (groupId-change ownership reconciliation).
- [x] 6.2 `pnpm verify` at the workspace root passes (tsc, biome incl. layer DAG, svelte-check, stylelint, catalog gates, vitest incl. story-parity guard).
